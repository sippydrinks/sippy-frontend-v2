import React from 'react'
import { ModalProps } from '@/interface'
import styles from './CheckoutPaymentModal.module.scss'

const CheckoutPaymentModal = ({isOpen, onClose}: ModalProps) => {
  return (
    <div>CheckoutPaymentModal</div>
  )
}

export default CheckoutPaymentModal