'use client';
import React, { useState } from 'react'

const useRecoverPassword = ({subTitle, otpError, handleInputChange, handleKeyDown, handlePaste, inputRefs, tokens, submitToken}: any) => {
  return (
    <div>
        
    </div>
  )
}

export default useRecoverPassword